[pytest]
# Compliance and shared-library tests only — service tests run per-service
# (each service has its own 'app' package; they can't share a Python path)
asyncio_mode = auto
testpaths =
    tests
    packages/py-shared/tests
pythonpath = packages/py-shared
